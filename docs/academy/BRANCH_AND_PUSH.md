# Feature Branch and Push

The classroom work is isolated on:

```text
student/academy-github-sfdx-workflow
```

The branch begins from the repository's current remote `main` branch. After review of the diff, it is pushed with:

```bash
git push -u origin student/academy-github-sfdx-workflow
```

`main` is not changed directly.
